          <%= form_tag chefs_path, method: :get do %>
          <%= text_field_tag :query,
            params[:query],
            class: "form-control",
            placeholder: "Search a cuisine type"
            %>
          <%= submit_tag "Search", class: "btn btn-primary" %>
          <% end %>

<div class= "container">
  <div class="row">
    <div class="col-md-8">
      <div class="row">
        <% @chefs.each do |chef| %>
          <div class="col-md-6">
            <div class="chef-card"> <img src="https://www.w3schools.com/bootstrap/la.jpg" class="img-fluid">
              <%= link_to chef_path(chef), class: "chef-card" do %>
                <h2><%= chef.nickname %></h2>
                <p> <%= chef.cuisine %></p>
                <p><%= chef.location %></p>
                <div class="chef-card-infos">
                  <h2 class="chef-card-pricing">120£</h2>
                  <img height="40" width="40" class="chef-card-avatar avatar-bordered" src="https://image.shutterstock.com/image-vector/chef-avatar-character-icon-260nw-690067834.jpg">
                </div>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
    <div class="col-md-4">
      <!-- MAP -->
      <div style="width: 100%; height: 600px;"
        data-controller="mapbox"
        data-mapbox-markers-value="<%= @markers.to_json %>"
        data-mapbox-api-key-value="<%= ENV['MAP_BOX_API_KEY'] %>">
      </div>
    </div>
  </div>
</div>
