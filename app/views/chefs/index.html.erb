<h1>Search chefs results </h1>
<div class="container">
  <div class="row justify-content-center">
    <div class="col-6">
    <% @chefs.each do |chef| %>
      <p><%= chef.nickname %></p>
      <p><%= chef.description %></p>

    <% end %>
      </div>
    </div>
</div>
